import React from 'react';
import { Link, Text } from '@chakra-ui/react'

const ParkingSpot = ({available, location, ...rest}) => {
    return (
        <>
        </>
    )
}

export default ParkingSpot;